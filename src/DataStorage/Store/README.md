# DataStorage Store

This directory will contain implementations that read and write opaque data.

The first store is selected with the first concrete consumer. Stores do not
decode or semantically modify the content.
