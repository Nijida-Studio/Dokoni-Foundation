# LocalSettings API

This directory will contain the interface used by other modules to query and
change local settings.

The concrete API remains open until the first consumer requirements are known.
