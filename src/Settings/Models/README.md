# Settings Models

`SettingsReadPlan`, `OperatingSystem`, and the stable settings defaults are
the first models. Concrete settings keys and values will be added with the
first agreed settings format.
