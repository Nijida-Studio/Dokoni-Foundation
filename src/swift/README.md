# Swift Libraries

This directory contains Swift implementations of accepted Dokoni Foundation
modules.

Foundation packages must not depend on SwiftUI. Apple-platform presentation and
application workflows remain in their owning application repositories.

No package has been created yet. The first package should be added only after
its module contract, consumers, supported platforms, and test boundary have
been agreed in `docs/modules.md`.
